#include <iostream>
#include <string>
#include <algorithm>
#include <vector>
#include <iomanip>

using namespace std;

void get_sub(vector<vector<int> > &vec, vector<int> num, int index1, int index2, int start, int k)
{
    if(index1 == k)
}

int main(int argc, char const *argv[])
{
    int m, k;
    cin >> m >> k;
    int num;
    vector<int> input;
    //读入数据
    for(int i =0; i < m; i++)
    {
        cin >> num;
        input.push_back(num);
    }
    //获取最小值最大的排列
    vector<vector<int> > vec;
    for()
    for(auto i : input)
        cout << i << ' ';
    cout << endl;
    return 0;
}